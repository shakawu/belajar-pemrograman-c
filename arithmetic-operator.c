#include <stdio.h>

int main()
{
    // Operasi aritmetika pada C

    int a = 5;
    int b = 10;
    int c = 0;

    // Penjumlahan
    // c = a + b;

    // Pengurangan
    // c = a - b;

    // Perkalian
    // c = a * b;

    // Pembagian
    // c = a / b;

    // Modulus
    // c = a % b;

    // Peningkatan Nilai
    // a++;

    // Penurunan Nilai
    // a++;

    // Augmented Assingment Operator
    a += b; // a = a + b

    return 0;
}